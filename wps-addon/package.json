{
  "name": "xstars-wps-addon",
  "addonType": "et",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "config:inject": "node scripts/inject-config.cjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check config.template.js && node --check service-client.js && node --check spreadsheet.js && node --check main.js"
  },
  "description": "Official XSTARS add-in for WPS Spreadsheet.",
  "devDependencies": {
    "et-jsapi-declare": "2.5.0",
    "vite": "8.2.2",
    "wpsjs": "2.2.3"
  }
}
